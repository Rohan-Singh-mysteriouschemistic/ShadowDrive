[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = -v --tb=short --strict-markers -x
markers =
    unit: Unit tests (no database or network)
    integration: Integration tests (require database)
    slow: Tests that take > 5 seconds
asyncio_mode = auto
